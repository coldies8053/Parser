# Parser — Lightweight Lemma-Based Question Router

A minimal **NLP parser** built with [spaCy](https://spacy.io/) that analyzes user input, lemmatizes it, and matches it against a set of predefined keyword dictionaries to produce smart text-based responses.

---

## Features
- Automatic text normalization (lowercase, punctuation cleanup, lemmatization)
- Lemma-based matching for more robust intent detection
- Flexible multi-dictionary system (`dictList`)
- Instant command-line interaction
